name: Android CI & Build Release

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main",Oto zunifikowany, zoptymalizowany plik konfiguracyjny GitHub Actions dla aplikacji Android (WinOls ECU Editor), integrujący automatyczną konfigurację środowiska JDK, uprawnienia wykonywania dla Gradle Wrappera, budowanie artefaktów (APK/AAB) oraz publikację wyników.

### .github/workflows/android.yml